# ============================================================================
#  FPS 多模态态势感知系统 —— Python 依赖清单
#  安装：pip install -r requirements.txt
# ============================================================================

# --- 核心依赖 ---
PyYAML>=6.0,<7.0          # YAML 配置文件解析
watchdog>=4.0.0,<5.0.0   # 文件系统事件监听，用于配置热重载
numpy>=1.24.0            # 图像数组运算
opencv-python>=4.8.0     # 图像预处理（灰度化等）
mss>=9.0.0               # 高性能屏幕捕获（ROI 抓取）
soundcard>=0.4.3         # WASAPI 环回采集（系统扬声器无损抓取）

# --- 开发 / 测试依赖 ---
pytest>=8.0.0,<9.0.0     # 单元测试框架

# Phase 1 (data engineering base) runtime + test dependencies.
# Later phases add torch/torchaudio/librosa (extras: [ml]) and
# fastapi/streamlit (extras: [api]) — see pyproject.toml.
pydantic>=2.5
pyyaml>=6.0
numpy>=1.26
pandas>=2.0
soundfile>=0.12
pytest>=8.0
